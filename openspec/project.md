# OpenSpec 项目说明

外脑（ExoBrain）的 OpenSpec 工作区：`specs/` 存放能力规范，`changes/` 存放进行中的 delta 提案。

当前仅预留结构（票据 17）；自我改进回路（feedback 识别 → propose → verify → archive）在 MVP 后由票据 27 启用，届时以周复盘会话驱动。OpenSpec 工具本体以 git submodule 形式挂在 `submodule/openspec`。
