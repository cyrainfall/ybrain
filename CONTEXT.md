# 术语表（外脑项目）

本文件只定义领域术语，不含实现细节。架构决策见 [.scratch/exobrain/](.scratch/exobrain/)。

## 外脑（ExoBrain）

在大脑之外持久保存想法、加快信息到知识的转化、激发想法连接的个人系统。由两部分构成：**数字扩展记忆**（可被快速唤醒的个人资料库）与**扩展智力**（以大语言模型为引擎的处理与对话能力）。

## 笔记库（Vault）

外脑的记忆本体：一个 Markdown 文件库，每条知识是一个带元数据头（frontmatter）的 .md 文件，按 PARA 组织。笔记库是普通文件夹，可被 Obsidian 等工具直接打开，不依赖外脑服务存在。

## 笔记（Note）

笔记库中的一个知识单元。来源可以是网页剪藏、手机速记、微信读书划线、代理生成的摘要或本人手写。

## 收件箱（Inbox）

笔记库中新捕获内容的落脚点：`0-Inbox/`。内容先进收件箱、保持原文，等待提炼；提炼后移动到 PARA 对应位置。捕获动作永远先落地收件箱，不依赖智能处理是否可用。

## 捕获（Capture）

把外部内容（网页、灵感、书摘）送进收件箱的动作，入口有浏览器剪藏、Android 分享菜单、微信读书同步三条渠道。

## 提炼（Distill）

对收件箱内容做增值处理：摘要、关键词、要点卡片、PARA 归类、与旧笔记建立连接。提炼后笔记从"原料"变成可检索、可连接的知识。

## 常驻代理（Agent）

运行在 ybrain 中的 AI 代理（opencode 本体），是外脑的"扩展智力"执行者：负责提炼知识、回答提问、整理笔记库；通过知识工具检索记忆、读写笔记。本人通过它的网页界面与外脑对话。

## ybrain（外脑服务）

服务器上的核心服务：opencode 的 fork（cyrainfall/ybrain）经二次开发后的成品。在原代理能力之上加入：捕获接口、知识原生工具、任务队列、嵌入索引。它既是"扩展智力"（代理），也是记忆的管理者（索引与调度），TypeScript/Bun 单进程。

## 知识工具

ybrain 内注册的原生代理工具（语义检索知识库、读取/保存笔记、查看收件箱等），代理用它们访问与维护记忆；捕获渠道通过 HTTP 接口调用 ybrain。

## PARA

笔记的组织法：项目（Projects，短期在做的事）、领域（Areas，长期关注）、资源（Resources，待用素材）、存档（Archive，不活跃内容的"时间胶囊"，靠检索而非分类唤醒）。

## 组网（Tailnet）

服务器与本人设备之间的私有加密网络（Headscale + Tailscale），外脑服务只在这个网络里可见，不裸露在公网。

## OpenSpec / 自我改进回路

OpenSpec 是规范驱动开发框架（git submodule 引入 ybrain fork）。外脑跑通后，用户日常对话中表达的抱怨/愿望被代理识别写入 feedback，周复盘会话用 `/opsx:propose` 自动生成改进提案（含验证方式），用户审阅后参数类改动由 distiller 自动 apply→verify→archive（Git 可回滚），功能类改动毕业为构建票据。delta-spec 归档时合并到主规范，形成完整行为演进史。MVP 只预留 `openspec/` 空目录和 feedback 识别，完整回路在 MVP 后启用。
